const fs = require('fs');
const path = require('path');

const filePath = path.join(__dirname, 'admin', 'src', 'pages', 'Admin', 'DoctorsList.jsx');
let content = fs.readFileSync(filePath, 'utf8');

const medDocsTab = `\r\n              {/* Medical Documents Tab */}\r\n              {detailsTab === 3 && (\r\n                <Box>\r\n                  <Box display="flex" gap={2} mb={3} flexWrap="wrap">\r\n                    {['pending','verified','rejected'].map(s => {\r\n                      const count = medicalDocs.filter(d => d.verificationStatus === s).length;\r\n                      return (\r\n                        <Paper key={s} elevation={1} sx={{ px: 2, py: 1, borderRadius: 3, display: 'flex', alignItems: 'center', gap: 1 }}>\r\n                          <Box sx={{ width: 10, height: 10, borderRadius: '50%', bgcolor: s==='verified'?'#10b981':s==='rejected'?'#ef4444':'#f59e0b' }} />\r\n                          <Typography variant="caption" fontWeight="bold" sx={{ textTransform: 'capitalize' }}>{s}: {count}</Typography>\r\n                        </Paper>\r\n                      );\r\n                    })}\r\n                    <Paper elevation={1} sx={{ px: 2, py: 1, borderRadius: 3 }}>\r\n                      <Typography variant="caption" fontWeight="bold">Total: {medicalDocs.length}</Typography>\r\n                    </Paper>\r\n                  </Box>\r\n                  {medicalDocsLoading ? (\r\n                    <Box display="flex" justifyContent="center" p={4}>\r\n                      <CircularProgress color="primary" />\r\n                    </Box>\r\n                  ) : medicalDocs.length === 0 ? (\r\n                    <Box textAlign="center" py={6}>\r\n                      <FolderIcon sx={{ fontSize: 56, color: '#c7d2fe', mb: 2 }} />\r\n                      <Typography variant="h6" color="text.secondary">No documents uploaded yet</Typography>\r\n                      <Typography variant="body2" color="text.disabled">The doctor has not uploaded any medical documents</Typography>\r\n                    </Box>\r\n                  ) : (\r\n                    <Box sx={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill,minmax(260px,1fr))', gap: 2 }}>\r\n                      {medicalDocs.map(doc => (\r\n                        <Paper\r\n                          key={doc._id}\r\n                          elevation={2}\r\n                          sx={{\r\n                            borderRadius: 3, overflow: 'hidden',\r\n                            border: '2px solid',\r\n                            borderColor: doc.verificationStatus === 'verified' ? '#bbf7d0' :\r\n                                         doc.verificationStatus === 'rejected' ? '#fecaca' : '#fef3c7',\r\n                            transition: 'box-shadow 0.2s',\r\n                            '&:hover': { boxShadow: '0 8px 24px rgba(0,0,0,0.12)' }\r\n                          }}\r\n                        >\r\n                          <Box sx={{ height: 140, bgcolor: '#f8fafc', display: 'flex', alignItems: 'center', justifyContent: 'center', position: 'relative', overflow: 'hidden' }}>\r\n                            {doc.fileType === 'image' ? (\r\n                              <img src={doc.url} alt={doc.name} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />\r\n                            ) : doc.fileType === 'pdf' ? (\r\n                              <Box textAlign="center">\r\n                                <PictureAsPdfIcon sx={{ fontSize: 52, color: '#ef4444' }} />\r\n                                <Typography variant="caption" display="block" color="text.secondary" fontWeight="bold">PDF</Typography>\r\n                              </Box>\r\n                            ) : (\r\n                              <Box textAlign="center">\r\n                                <InsertDriveFileIcon sx={{ fontSize: 52, color: '#6366f1' }} />\r\n                                <Typography variant="caption" display="block" color="text.secondary" fontWeight="bold">\r\n                                  {doc.mimeType?.split('/')[1]?.toUpperCase() || 'FILE'}\r\n                                </Typography>\r\n                              </Box>\r\n                            )}\r\n                            <Chip\r\n                              label={doc.verificationStatus?.toUpperCase()}\r\n                              size="small"\r\n                              sx={{\r\n                                position: 'absolute', top: 8, right: 8, fontSize: 9, fontWeight: 'bold',\r\n                                bgcolor: doc.verificationStatus === 'verified' ? '#10b981' :\r\n                                         doc.verificationStatus === 'rejected' ? '#ef4444' : '#f59e0b',\r\n                                color: 'white'\r\n                              }}\r\n                            />\r\n                          </Box>\r\n                          <Box sx={{ p: 1.5 }}>\r\n                            <Typography variant="body2" fontWeight="bold" noWrap title={doc.name}>{doc.name}</Typography>\r\n                            <Box display="flex" alignItems="center" gap={1} mt={0.5} mb={1}>\r\n                              <Chip\r\n                                label={doc.category === 'education' ? '\\uD83C\\uDF93 Education' : doc.category === 'records' ? '\\uD83D\\uDCCB Records' : doc.category === 'govtId' ? '\\uD83E\\uDEAA Govt ID' : '\\uD83D\\uDCC1 Other'}\r\n                                size="small"\r\n                                sx={{ fontSize: 9, bgcolor: doc.category==='education'?'#dbeafe':doc.category==='records'?'#dcfce7':doc.category==='govtId'?'#f3e8ff':'#f1f5f9', fontWeight:'bold' }}\r\n                              />\r\n                              <Typography variant="caption" color="text.disabled">\r\n                                {doc.storageProvider === 'cloudinary' ? '\\u2601\\uFE0F' : '\\uD83D\\uDD25'}\r\n                              </Typography>\r\n                            </Box>\r\n                            {doc.adminNote && (\r\n                              <Typography variant="caption" color="text.secondary" display="block" sx={{ fontStyle: 'italic', mb: 1, borderTop: '1px solid #f1f5f9', pt: 0.5 }}>\r\n                                Note: {doc.adminNote}\r\n                              </Typography>\r\n                            )}\r\n                            <Typography variant="caption" color="text.disabled" display="block" mb={1.5}>\r\n                              Uploaded: {new Date(doc.uploadedAt).toLocaleDateString()}\r\n                            </Typography>\r\n                            <Box display="flex" gap={1} flexWrap="wrap">\r\n                              <Button\r\n                                size="small"\r\n                                variant="outlined"\r\n                                startIcon={<OpenInNewIcon />}\r\n                                href={doc.url}\r\n                                target="_blank"\r\n                                rel="noreferrer"\r\n                                sx={{ fontSize: 10, borderRadius: 2, flex: 1 }}\r\n                              >View</Button>\r\n                              <Button\r\n                                size="small"\r\n                                variant="contained"\r\n                                sx={{ fontSize: 10, borderRadius: 2, flex: 1, bgcolor: '#4f46e5', '&:hover': { bgcolor: '#4338ca' } }}\r\n                                onClick={() => {\r\n                                  setDocToVerify(doc);\r\n                                  setVerifyStatus(doc.verificationStatus || 'verified');\r\n                                  setVerifyNote(doc.adminNote || '');\r\n                                  setVerifyDialogOpen(true);\r\n                                }}\r\n                              >Verify</Button>\r\n                              <Button\r\n                                size="small"\r\n                                variant="contained"\r\n                                color="error"\r\n                                sx={{ fontSize: 10, borderRadius: 2, minWidth: 'auto', px: 1 }}\r\n                                onClick={() => handleAdminDeleteDoc(doc._id)}\r\n                              ><DeleteIcon sx={{ fontSize: 14 }} /></Button>\r\n                            </Box>\r\n                          </Box>\r\n                        </Paper>\r\n                      ))}\r\n                    </Box>\r\n                  )}\r\n                </Box>\r\n              )}\r\n`;

// Find the closing )} of activity logs tab, right before the </> fragment close
// The pattern: the three-line closing of the activity-logs block then the </> at detailsTab level
const MARKER = '              )}\r\n            </>';
const idx = content.indexOf(MARKER);

if (idx === -1) {
  // Try LF only
  const MARKER2 = '              )}\n            </>';
  const idx2 = content.indexOf(MARKER2);
  if (idx2 === -1) {
    console.error('MARKER NOT FOUND - checking raw snippet around line 1003');
    // Output lines 995-1008 as char codes for debugging
    const lines = content.split('\n');
    for (let i = 996; i <= 1007; i++) {
      const codes = [...lines[i]].map(c => c.charCodeAt(0));
      console.log(`Line ${i+1}: ${JSON.stringify(lines[i])} codes:`, codes.join(','));
    }
    process.exit(1);
  }
  const replacement = medDocsTab.replace(/\r\n/g, '\n') + '            </>';
  const newContent = content.substring(0, idx2 + '              )}\n'.length) + medDocsTab.replace(/\r\n/g, '\n') + '            </>';
  const after = content.substring(idx2 + MARKER2.length);
  fs.writeFileSync(filePath, content.substring(0, idx2 + '              )}\n'.length) + medDocsTab.replace(/\r\n/g, '\n') + '            </>' + after, 'utf8');
  console.log('Done (LF mode)');
} else {
  const replacement = content.substring(0, idx + '              )}\r\n'.length) + medDocsTab + '            </>';
  const after = content.substring(idx + MARKER.length);
  fs.writeFileSync(filePath, replacement + after, 'utf8');
  console.log('Done (CRLF mode)');
}
